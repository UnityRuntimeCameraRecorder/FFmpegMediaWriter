namespace Landoria.FFmpegMediaWriter
{
    // Identifies the video representation delivered by a capture backend to FFmpeg.
    public enum VideoStreamFormat
    {
        RawRgba,
        H264,
        Hevc
    }
}
